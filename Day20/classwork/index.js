const os =require("os"); //require is the common js

const http = require("http");
console.log(http.METHODS); //


// console.log(os.homedir());
// console.log(os.machine());
// console.log(os.cpus());
// console.log(os.freemem());